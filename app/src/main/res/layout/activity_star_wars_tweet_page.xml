<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:weightSum="10"
    android:layout_height="match_parent"
    android:background="@drawable/st_background"
    tools:context="com.developer.sarthak.updgeekverse.StarWarsTweetPage">


    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1.3">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|left"
            android:layout_margin="@dimen/fab_margin"
            android:background="@android:color/transparent"
            android:backgroundTint="@android:color/transparent"
            android:scaleType="center"
            android:src="@drawable/ic_close_white_24dp"
            app:borderWidth="0dp" />

        <Button
            android:id="@+id/st_post"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right|top"
            android:layout_margin="@dimen/fab_margin"
            android:background="@drawable/tweet_background"
            android:padding="18px"
            android:text="post"
            android:textColor="@android:color/black"
            tools:layout_editor_absoluteX="457dp" />
    </android.support.design.widget.CoordinatorLayout>

    <EditText
        android:layout_width="match_parent"
        android:id="@+id/st_tweet"
        android:layout_height="0dp"
        android:layout_weight="8"
        android:textColor="#ffffff"
        android:theme="@style/StarWarsTheme"
        android:textCursorDrawable="@drawable/star_wars_cursor"
        android:background="@android:color/transparent"
        android:gravity="top|left"
        android:layout_margin="@dimen/fab_margin" />

</LinearLayout>
